import type { Metadata } from "next";
import { Inter, Noto_Sans_Thai } from "next/font/google";
import "./globals.css";

// ⭐️ เปลี่ยนมา Import จากไฟล์ที่เราสร้างใหม่แทน
import { ThemeProvider } from "./components/theme-provider";

const notoSansThai = Noto_Sans_Thai({ subsets: ["thai", "latin"] });

export const metadata: Metadata = {
  title: "ระบบสืบค้นวิทยานิพนธ์",
  description: "North Bangkok University",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    // suppressHydrationWarning สำคัญมาก ต้องมีเพื่อไม่ให้ระบบเตือนเรื่องสี
    <html lang="th" suppressHydrationWarning>
      <body className={notoSansThai.className}>
        <ThemeProvider attribute="class" defaultTheme="system" enableSystem>
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}